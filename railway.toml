[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd src && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
